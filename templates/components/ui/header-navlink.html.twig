<a href="{{ href }}"
    class="header-navlink text-xs text-center italic text-shadow-xs/25 cursor-pointer relative after:absolute after:-bottom-1
    after:left-0 after:transition-all after:duration-150 after:ease-in-out after:content-['_'] after:h-[1px] after:w-0
    after:bg-primary hover:after:w-full {% if class is defined %}{{ class }}{% endif %}"
>{{ text }}</a>
